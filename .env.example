# No environment variables are required to run this project.
# Everything is processed in the browser; there is no backend, database, or auth.
#
# Optional — overrides the canonical origin used for metadata, sitemap, and robots.
# Defaults to https://divyanshupatel.com when unset.
NEXT_PUBLIC_SITE_ORIGIN=
